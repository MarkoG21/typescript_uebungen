function addieren(zahl1: number, zahl2: number, showResult: boolean) {
  if (showResult) {
    return zahl1 + zahl2;
  } else {
    return zahl22;
  }
}

let gehalt: number;
const zahl22 = 2;
const zahl2 = 2.3;

const result = addieren(zahl22, zahl2, false);
console.log(result);
